<form method="post" action="{{ path('app_etudiant_delete', {'nce': etudiant.nce}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etudiant.nce) }}">
    <button class="btn btn-danger">Delete</button>
</form>
